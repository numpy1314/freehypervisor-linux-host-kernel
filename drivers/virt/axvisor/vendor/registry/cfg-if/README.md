Local registry snapshot for `cfg-if`.

Reason for inclusion:

- required by `ax-percpu-macros`
- also required by multiple later runtime/virtualization crates

Provenance:

- copied from local Cargo registry / existing local vendor snapshot
